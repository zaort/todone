<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body class="flex justify-center  font-sans font-light bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-slate-50 to-slate-200">
    {{#if loggedIn}}
    <div class="flex justify-center items-center w-11/12 h-screen rounded-lg">
        <div class="flex h-5/6 rounded-lg shadow-xl shadow-slate-300">
            <nav
                class="w-44 border-r border-r-slate-300 flex flex-col bg-gray-700 text-center text-slate-50 pt-5 rounded-l-md">
                <div class="border-b border-b-slate-300 rounded-b-md flex justify-center">
                    <h1 class="text-3xl font-bold mb-4">ToDone</h1>
                </div>
                <div class="flex-col w-full justify-center text-center">
                    <button id="addTodoBtn"
                        class="0 w-11/12 text-lg font-semibold text-slate-50 py-1 rounded-md mt-4 hover:bg-gray-500 focus:outline-none focus:ring focus:ring-slate-400">
                        Add Todo
                    </button>
    
                    <button id="logoutBtn"
                        class="0 w-11/12 text-lg font-semibold text-slate-50 py-1 rounded-md mt-4 hover:bg-gray-500 focus:outline-none focus:ring focus:ring-slate-400">
                        Logout
                    </button>
                </div>
            </nav>
                                                        {{!-- todo list container --}}
                                                        {{!-- Verify that it is taking the #each inside the task body partials --}}
        {{#if list.length}}
                <div class="flex-1 p-8 overflow-hidden bg-slate-50 rounded-r-md">
                    <div class="flex items-center">
                        <h2 class="text-2xl text-start font-bold mb-4">Todo List</h2>
                    </div>
                    {{> taskbody}}
                </div>
        {{/if}}
        </div>
    </div>
    
    {{/if}}
    {{{body}}}
    <script src="../../public/js/sessionsPage.js"></script>
    <script src="../../public/js/todoPage.js"></script>
</body>
</html>